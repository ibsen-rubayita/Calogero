# MongoDB connection string for the backend
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/construction?retryWrites=true&w=majority

# SMTP credentials for sending email notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=your-smtp-password

# The email address that receives new appointment requests
ADMIN_EMAIL=office@example.com
